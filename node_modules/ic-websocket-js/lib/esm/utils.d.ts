import { HttpAgent } from "@dfinity/agent";
import { Principal } from "@dfinity/principal";
export declare const isMessageBodyValid: (canisterId: Principal, path: string, body: Uint8Array | ArrayBuffer, certificate: ArrayBuffer, tree: ArrayBuffer, agent: HttpAgent, maxCertificateAgeInMinutes: number) => Promise<boolean>;
export declare const safeExecute: <T>(fn: () => T | Promise<T>, warnMessage: string) => Promise<T | undefined>;
/**
 * Generates a random unsigned 64-bit integer
 * @returns {bigint} a random bigint
 */
export declare const randomBigInt: () => bigint;
